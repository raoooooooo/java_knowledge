# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 提供本仓库的操作指引。

## 项目概述

这是一个Java后端开发知识总结文档项目，专门用于**面试准备**。包含6年Java工作经验相关知识总结。

- **主要用途**：面试准备
- **组织方式**：按技术栈分类组织
- **内容类型**：核心概念 + 常见面试题
- **文档格式**：纯Markdown文件（每个技术主题一个文件）

## 目录结构

```
java_knowledge/
├── README.md                          # 项目概览和知识大纲
├── 01-java基础/                       # Java基础知识
├── 02-JVM虚拟机/                      # JVM虚拟机
├── 03-Spring全家桶/                   # Spring框架
├── 04-数据库/                         # 数据库知识
├── 05-分布式系统/                     # 分布式系统
├── 06-中间件/                         # 中间件
├── 07-算法与数据结构/                 # 算法与数据结构
├── 08-设计模式/                       # 设计模式
└── 09-系统设计与架构/                 # 系统设计与架构
```

## 常用命令

本项目为纯文档项目：

1. **查看现有文档**：对任意 `.md` 文件使用 Read 工具
2. **创建新文档**：在对应目录下遵循 `XX-文件名.md` 命名规范
3. **更新现有文档**：使用 Edit 工具修改内容
4. **检查文件结构**：`find . -name "*.md"` 列出所有Markdown文件

## 编写规范

> ⚠️ **铁律1：每次新增/删除/重命名知识点文件时，必须同步更新 README.md 中的知识大纲目录**

> ⚠️ **铁律2：严禁将隐私信息提交到GitHub仓库。提交前必须检查并移除：个人姓名、公司名称、IP地址、邮箱、手机号、内部URL、Token、密钥等任何敏感或隐私信息**

> ⚠️ **铁律3：阅读参考资料（docx/pdf/网页等）并输出文档时，必须对资料内容的正确性进行判别，不能照搬照抄。** 资料中可能存在表述不准、因果牵强、概念混淆、笔误甚至错误的知识点。对于识别出的错误或不严谨之处：① 在正文相应位置就地修正（可加 `⚠️` 标注）；② 在文件末尾用「资料勘误与重点提醒」小节集中强调说明。特别要补充资料中遗漏但面试高频的重点（如经典问题、底层机制、演进原因）。判断依据以官方文档与业界共识为准。
>
> ⚠️ **铁律4：所有 mermaid 图表的 subgraph 必须使用「英文ID + 中文标签」格式，确保 GitHub 兼容性。** 即 `subgraph english_id["中文显示名"]`，禁止直接使用中文（尤其是含括号/冒号等特殊字符的中文）作为 subgraph ID。跨 subgraph 连线必须引用英文 ID。新增或修改 mermaid 图时必须遵守此规范。

- **文件命名**：使用数字前缀 + 中文名称，例如 `01-集合框架.md`
- **目录命名**：使用数字前缀目录以保持顺序
- **内容格式**：每个文件包含「核心概念」和「常见面试题」两个部分
- **渐进式填充**：先列出知识点大纲，具体内容留空待后续学习补充
- **README同步**：任何目录结构或文件的变更，必须同步刷新 README.md 对应目录
- **Mermaid 图表**：所有 subgraph 统一使用 `subgraph english_id["中文标签"]` 格式，禁止直接用中文/特殊字符作为 subgraph ID，确保 GitHub 渲染兼容

---

## Git 提交规范

### Commit 格式

```
<type>(<scope>): <中文简短描述>

<中文详细正文>

<中文备注或关联 Issue>
```

### 格式说明

1. **type/scope**：知识大模块分类，如：
   - `java` - Java基础
   - `jvm` - JVM虚拟机
   - `spring` - Spring全家桶
   - `db` - 数据库
   - `distributed` - 分布式系统
   - `middleware` - 中间件
   - `algorithm` - 算法与数据结构
   - `design` - 设计模式
   - `architecture` - 系统设计与架构

2. **中文简短描述**：一句话概述本次提交内容

3. **中文详细正文**：此次提交新增/修改文档的简要描述（可选）

4. **中文备注或关联 Issue**：有关联 Issue 时添加
   - 格式示例：`关闭 #123`、`修改 #456`、`优化 #789`
   - 操作关键词：关闭、修改、优化、打开等

### 提交示例

```
docs(java): 新增并发编程知识点大纲

- 新增线程池核心参数与工作原理
- 新增 synchronized 与 volatile 对比
- 新增 AQS 原理与常见面试题

关闭 #5
```
